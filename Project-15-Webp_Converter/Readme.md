# 🖼️ Image to WebP Converter

A blazing fast Python automation script that converts bulk images (`.jpg`, `.png`, `.jpeg`) into highly optimized **WebP** format.

## 🚀 Features
* **Bulk Processing:** Converts hundreds of images in seconds.
* **Smart Compression:** Reduces file size (~80% quality) without visible loss.
* **Auto-Organization:** Creates a clean `converted_wb` folder for output.
* **Robust:** Skips corrupted files automatically without crashing.

## 🛠️ Installation

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/BerkeKaracan/Python-Projects.git
    ```
2.  **Install Requirements:**
    ```bash
    pip install Pillow
    ```

## 🎮 How to Use

1.  Run the script:
    ```bash
    python main.py
    ```
2.  Paste the **folder path** containing your images when prompted.
3.  Done! Check the `converted_wb` folder inside your target directory.

---
*Developed by [Berke Karacan]*